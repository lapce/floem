[package]
name = "widget-gallery"
version = "0.1.0"
edition = "2021"

[dependencies]
floem = { path = "../..", features = ["vello"] }
strum = { workspace = true }
files = { path = "../files/", optional = true }

[features]
default = ["full"]
vello = ["floem/vello"]
full = ["dep:files"]
